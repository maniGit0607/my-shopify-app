{
  "name": "my-shopify-app",
  "version": "1.0.0",
  "description": "Shopify reporting app with Hono backend and React frontend",
  "private": true,
  "workspaces": [
    "ui",
    "backend"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:*",
    "dev:ui": "cd ui && npm run dev",
    "build": "npm-run-all build:*",
    "build:ui": "cd ui && npm run build",
    "install:all": "npm install && cd ui && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
